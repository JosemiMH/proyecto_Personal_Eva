# Database Configuration
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# OpenAI API (for chatbot functionality)
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Email Configuration (Brevo/Sendinblue)
EMAIL_USER=your-email@smtp-brevo.com
EMAIL_PASS=your-smtp-api-key-or-password
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_SECURE=false
EMAIL_FROM=eva@evaperez-wellness.com

# Environment
NODE_ENV=production

# Server Configuration
# Hostinger uses PORT=3000 by default
PORT=3000

# Session Secret (CRITICAL for production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-very-long-random-secret-minimum-32-characters-here
